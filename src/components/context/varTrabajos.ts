// Este archivo quedó retirado: los casos de portafolio que alimentaban a
// CoTrabajos (home y works) ahora viven en public/data/works.json, leídos en
// runtime por useWorksData.ts; la media (cover/gif/logos) se movió a
// worksAssets.ts.
//
// No se pudo borrar este archivo del disco al hacer el cambio (una
// limitación temporal del entorno impidió ejecutar comandos en el equipo),
// así que se dejó vacío para no romper el type-check — es seguro borrarlo
// por completo (clic derecho > eliminar en tu editor) cuando quieras.
export {};
